import { ComingSoon } from '@/components/screen';

export default function Workout() {
  return (
    <ComingSoon
      title="Workout"
      note="Phase 2 puts workout logging here: start a session, pick exercises, and add sets."
    />
  );
}
